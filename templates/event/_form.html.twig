{{ form_start(form) }}
    {{ form_row(form.date) }}
    {{ form_row(form.city) }}
    {{ form_row(form.title) }}
    {{ form_row(form.description) }}
    {{ form_row(form.picture) }}
<div class="listing">
    {{ form_row(form.members) }}
</div>
<div class="d-flex justify-content-center">
    <button class="btn btn-secondary mt-5 mb-5">
        <i class="fas fa-save mr-2"></i> {{ button_label|default('Ajouter') }}
    </button>
</div>
{{ form_end(form) }}
